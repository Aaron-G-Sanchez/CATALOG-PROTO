{
  "name": "trpc-proto",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5 || ^6"
  },
  "private": true,
  "scripts": {
    "dev:server": "bun run --watch apps/server/src/index.ts",
    "dev:frontend": "bun run --cwd apps/frontend dev"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
